# Copiloto de Transparencia de Facturación (Lucía) - dependencias del backend
#
# Generado a partir de los imports reales del proyecto (no de un pip freeze
# completo, que incluye sub-dependencias transitivas de langchain/langgraph
# que no se usan directamente aquí).

# --- API / Core ---
fastapi
uvicorn
starlette

# --- Validación y configuración ---
pydantic
pydantic-settings
python-dotenv

# --- Base de datos ---
SQLAlchemy
# Driver de PostgreSQL. Necesario cuando DATABASE_URL apunta a Supabase; con
# SQLite no se usa, pero se instala para que el mismo requirements sirva en local
# y en producción.
# psycopg2-binary

# --- Capa RAG (Supabase + pgvector) ---
supabase

# Embeddings locales para EMBEDDING_PROVIDER=local (all-MiniLM-L6-v2, 384 dims).
# Runtime ONNX, ~80 MB, sin API externa ni costo por token.
fastembed
# Alternativa más pesada al anterior (~2 GB con torch). Solo si prefieres el
# ecosistema de sentence-transformers; el proyecto detecta cuál está instalado.
#   pip install sentence-transformers

# --- LLM (DeepSeek vía interfaz compatible con OpenAI) ---
langchain-core
langchain-openai

# --- HTTP saliente (WhatsApp Cloud API, health checks) ---
requests

# --- Bot de Telegram (scripts/telegram_bot.py) ---
python-telegram-bot

# --- Procesamiento de Datos ---
pandas
openpyxl
psycopg2-binary

# --- Infografías (visual_1/2/3: variación, desglose, histórico del recibo) ---
# Usado por app/services/image_renderer.py para generar las imágenes PNG que
# se envían por WhatsApp. Se eligió Pillow (dibujo directo) en vez de un
# navegador headless (Playwright) para no depender de descargar un binario de
# Chromium en el servidor de despliegue.
Pillow
